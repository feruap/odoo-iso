<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Acción para generar lotes de fábrica desde el menú de acciones -->
    <record id="action_generate_factory_lots" model="ir.actions.server">
        <field name="name">Generar Lotes de Fábrica</field>
        <field name="model_id" ref="stock.model_stock_move_line"/>
        <field name="binding_model_id" ref="stock.model_stock_move_line"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
if records:
    action = records.action_generate_factory_lots()
        </field>
    </record>
</odoo>
