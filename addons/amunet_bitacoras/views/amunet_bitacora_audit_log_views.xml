<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_bitacora_audit_log_list" model="ir.ui.view">
        <field name="name">amunet.bitacora.audit.log.list</field>
        <field name="model">amunet.bitacora.audit.log</field>
        <field name="arch" type="xml">
            <list string="Auditoría de bitácoras" create="false" edit="false">
                <field name="create_date"/>
                <field name="entry_id"/>
                <field name="event"/>
                <field name="user_id"/>
                <field name="detail"/>
            </list>
        </field>
    </record>

    <record id="action_bitacora_audit_log" model="ir.actions.act_window">
        <field name="name">Auditoría</field>
        <field name="res_model">amunet.bitacora.audit.log</field>
        <field name="view_mode">list</field>
    </record>
</odoo>
