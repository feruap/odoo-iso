<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Usuarios normales: solo ven los planes donde están en el equipo -->
    <record id="rule_plan_auditoria_equipo" model="ir.rule">
        <field name="name">Plan auditoría: visible solo al equipo del plan</field>
        <field name="model_id" ref="model_amunet_plan_auditoria"/>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="domain_force">
            ['|', '|', '|',
                ('lider_id', '=', user.id),
                ('auditor_ids', 'in', [user.id]),
                ('observador_ids', 'in', [user.id]),
                ('experto_ids', 'in', [user.id])
            ]
        </field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Gestora de documentación: ve y gestiona todos los planes -->
    <record id="rule_plan_auditoria_manager" model="ir.rule">
        <field name="name">Plan auditoría: acceso total para documentación</field>
        <field name="model_id" ref="model_amunet_plan_auditoria"/>
        <field name="groups" eval="[(4, ref('amunet_documentos.group_documentos_manager'))]"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

</odoo>
